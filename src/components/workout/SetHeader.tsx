
import React from 'react';
import SetHeader from './set/SetHeader';

/**
 * Legacy wrapper for backward compatibility
 */
const WorkoutSetHeader: React.FC = () => {
  return <SetHeader />;
};

export default WorkoutSetHeader;
